/// Standard corner radii used across the app for a consistent shape language.
class AppRadius {
  AppRadius._();

  /// Cards and card-like surfaces.
  static const double card = 16;

  /// Inputs, tiles and small inner elements.
  static const double field = 12;

  /// Pills, chips and fully-rounded controls.
  static const double pill = 20;
}
